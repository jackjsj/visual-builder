import Vue from 'vue';
import {
  Form,
  Input,
  Divider,
  InputNumber,
  Switch,
  Tabs,
  Select,
  Button,
  Alert,
  Icon,
  Dropdown,
  Menu,
} from 'ant-design-vue';

Vue.use(Form)
  .use(Input)
  .use(Divider)
  .use(InputNumber)
  .use(Tabs)
  .use(Select)
  .use(Button)
  .use(Alert)
  .use(Icon)
  .use(Dropdown)
  .use(Menu)
  .use(Switch);

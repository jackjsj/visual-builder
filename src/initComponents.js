import Vue from 'vue';
import {
  Form,
  Input,
  Divider,
  InputNumber,
  Switch,
  Tabs,
  Select,
  Button,
  Alert,
  Icon,
} from 'ant-design-vue';

Vue.use(Form)
  .use(Input)
  .use(Divider)
  .use(InputNumber)
  .use(Tabs)
  .use(Select)
  .use(Button)
  .use(Alert)
  .use(Icon)
  .use(Switch);
